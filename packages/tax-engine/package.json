{
    "name": "@antigravity/tax-engine",
    "version": "1.0.0",
    "description": "Core tax calculation engine for Nigerian 2025 reforms",
    "main": "./dist/index.js",
    "module": "./dist/index.mjs",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsup src/index.ts --format cjs,esm --dts",
        "dev": "tsup src/index.ts --format cjs,esm --watch --dts",
        "lint": "eslint \"src/**/*.ts*\"",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "tsup": "^8.0.2",
        "typescript": "^5.3.3",
        "rimraf": "^5.0.5"
    }
}
